# CoinOPS Dolphin core options
#
# Dolphin registers its option definitions during retro_load_game(), so the
# generic CoinOPS build-time option dumper cannot enumerate them without
# attempting to boot content. This file intentionally contains overrides only.
# Any option not listed here uses the default compiled into the pinned core.
#
# Add tested Dolphin option overrides here as needed.
